import React from 'react'

const CreatePost = ({prev}) => {
  return (
    <div>
        CreatePost
        <button onClick={prev}>prev</button>
        <button>Submit</button>
        </div>
    
  )
}

export default CreatePost